import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFFFFC0CB); // Soft Pink
  static const Color secondColor = Color(0xFFFFA500); // Orange
  static const Color primaryWhite = Color(0xFFFFFFFF); // White

  // Background Colors
  static const Color lightBackground = Color(0xFFFFf0ff);
  static const Color darkBackground = Color(0xFF121212);

  // Text Colors
  static const Color lightText = Color(0xFF000000); // Black for Light Mode
  static const Color darkText = Color(0xFFFFFFFF); // White for Dark Mode

  static const Color grey = Color(0xFFBDBDBD);
  static const Color darkGrey = Color.fromARGB(255, 128, 126, 126);
  static const Color black = Color(0xFF000000);
}
